SELECT
    col1,
    col2,
    CAST(SUBSTR(col3, 7, 4) || '-' || SUBSTR(col3, 4, 2) || '-' || SUBSTR(col3, 1, 2) AS DATE),
    col4,
    col5,
    col6,
    col7,
    col8,
    col9,
    col10,
    CAST(SUBSTR(col11, 7, 4) || '-' || SUBSTR(col11, 4, 2) || '-' || SUBSTR(col11, 1, 2) AS DATE),
    col12,
    col13
